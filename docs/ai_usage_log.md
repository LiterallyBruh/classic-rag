# Лог использования AI-инструментов в разработке

Конкурс поощряет применение AI-инструментов (критерий №3). Здесь фиксируется,
что и как делалось с помощью LLM — честно и проверяемо.

| Дата | Инструмент | Задача | Результат / проверка |
|---|---|---|---|
| 2026-07-06 | Claude | Каркас репозитория, скелеты модулей | Ревью и доработка вручную |
| | | Черновик eval-вопросов из текста глав | Ручная валидация каждого вопроса |
| | | Генерация перефразировок вопросов для стресс-теста retrieval | |
| 2026-07-06 | Claude (Cowork) | CI: actions/checkout и setup-python → v6; проверка URL корпуса по индексу az.lib.ru, исправлен URL «Фауста» (text_1800_faust, пер. Холодковского); в download_texts добавлена проверка минимального размера | ruff + pytest зелёные локально; скачивание текстов выполняется локально (сэндбокс без доступа к az.lib.ru) |
| 2026-07-06 | Claude Code | Скачивание корпуса; переписан парсер очистки (src/classic_rag/cleaning.py): строчный state-machine под вёрстку az.lib.ru, отдельный парсер «Фауста» (сцены/реплики), тесты | Валидация структуры по всем 4 книгам (полнота частей/глав, отсутствие мусора, smoke-тест знаменитых цитат); найдено и задокументировано ограничение: перевод Холодковского в открытых источниках сокращённый (D-004) |
| 2026-07-06 | Claude Code | EDA-ноутбук (notebooks/01_eda.ipynb): сводка корпуса, полнота разметки, распределения длин, частоты имён; пополнение configs/characters.yaml по частотам | Ноутбук выполнен end-to-end (nbconvert --execute); покрытие топ-15 имён словарём проверено в самом ноутбуке (15/15, 12/15, 12/15 — остаток только падежные формы); тесты нормализатора зелёные |
| 2026-07-06 | Claude Code | Чанкинг (src/classic_rag/chunking.py, 3 стратегии) и индексация (build_index.py, retrieval.py: персистентный индекс, ленивый энкодер, BM25-токенизация razdel) | Юнит-тесты стратегий и ретривера с фейковым энкодером (18 тестов зелёные); найден и исправлен баг неограниченного роста окна при переносе перекрытия; smoke-тест BM25-поиска на реальном индексе |
| 2026-07-06 | Claude Code | Reranker (rerank.py, bge-reranker-v2-m3, ленивая загрузка), пайплайн (pipeline.py: нормализация → гибрид → reranker → генерация), генерация с инъецируемым клиентом и отказом без фрагментов; обновлено Streamlit-демо | Тесты с фейковыми скорером/LLM (24 зелёные); smoke-тест retrieve+rerank на реальных вопросах; решение D-008 |
| 2026-07-06 | Claude Code | Streamlit-демо (режим без LLM-ключа, примеры вопросов), scripts/bootstrap.py, Docker (CPU-only torch, .dockerignore, $PORT для HF Spaces), docs/deploy.md, актуализация README | Демо проверено локально (HTTP 200) и в контейнере (образ 2.4 ГБ, HTTP 200); пойман раздув образа CUDA-колёсами torch — исправлено установкой с CPU-индекса |
| 2026-07-06 | Claude Code | Eval-набор: 120 вопросов (по 30/книгу, factual/quote/interpretive) с золотыми локациями и проверочными подстроками; scripts/verify_eval.py; eval/run_eval.py (recall@k span/loc, MRR) | КАЖДЫЙ вопрос программно верифицирован против корпуса (120/120: gold_match дословно найден в золотой главе/сцене); черновые локации уточнялись по фактическому тексту, 4 исправлены после первого прогона верификатора; попутно найдены и исправлены 2 бага парсера «Фауста» (однобуквенная «сцена Я», двухстрочный заголовок сцены) |
| 2026-07-07 | Claude Code | Подключение GigaChat: фабрика LLM-клиентов (llm.py, кэш OAuth-токена, httpx.Auth), генерация переведена на фабрику, .env.example; скрипт деплоя на HF Spaces (deploy_hf_space.sh: сборка содержимого Space из основного репо, git-lfs для индекса), ранний выход bootstrap при готовом индексе | Юнит-тесты кэша токена и выбора клиента (28 зелёных); живой end-to-end вызов GigaChat через answer() — ответ с цитатой и адресом главы; решения D-009, D-010 |
| 2026-07-07 | Claude Code | Диагностика отказа на живом демо («последняя фраза Мышкина»): позиционные вопросы vs similarity-поиск; модуль structural.py (интент-правила, срез головы/хвоста книги, справка для LLM о позиции фрагментов), маршрутизация в pipeline | Воспроизведено на реальном индексе до/после: retrieval стал приносить финал (ч.4 гл.11 + Заключение вместо части 1); «Чем заканчивается Фауст?» отвечается корректно с цитатой; проверка триггеров на 120 eval-вопросах (4 срабатывания — все корректны после сужения правил); 34 теста зелёные; решение D-011 |
| 2026-07-07 | Claude Code | Сравнение с baseline (eval/compare_baseline.py): GigaChat без RAG vs ClassicLiteratureRAG на 29 вопросах, автометрики (дословность цитат по корпусу, леммная сверка существа ответа pymorphy3, номер главы), режим --rescore по сохранённым ответам | Три итерации метрики с разбором ложных срабатываний (кавычки вокруг адресов; морфология «погиб/погибает»; цитата в самом вопросе); итог: baseline 0/27 дословных цитат и 2/15 верных глав против 8/9 и 12/15 у RAG; отчёт eval/baseline_comparison.md, сырые ответы для аудита; решение D-012 |
| 2026-07-07 | Claude Code | Механизм фидбека: src/classic_rag/feedback.py (локальный JSONL + опциональное дублирование в приватный HF-датасет), форма 👍/👎 в демо, кэш ответа в session_state | Тест записи JSONL (35 зелёных); смоук Streamlit локально (HTTP 200); решение D-013 |
| 2026-07-07 | Claude Code | Конкурсное описание docs/description.md по требуемой структуре («задача, данные, методы, результаты», до 3 стр.) и генератор PDF (scripts/build_description_pdf.py, DejaVu для кириллицы) | Все цифры взяты из eval/report.md и eval/baseline_comparison.md; PDF — 2 страницы, вычитка постранично по рендерам |
