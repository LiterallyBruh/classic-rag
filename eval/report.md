# Отчёт по оценке retrieval

## Методика

- **Набор**: `eval/questions.jsonl` — 120 вопросов (по 30 на книгу), три типа:
  factual (факты сюжета), quote (атрибуция цитат), interpretive (интерпретация).
  Каждый вопрос имеет золотую локацию (часть+глава / часть+сцена) и
  `gold_match` — подстроку, дословно присутствующую в тексте этой локации.
  Валидность 120/120 проверяется программно: `scripts/verify_eval.py`.
- **Метрики** (`eval/run_eval.py`):
  - `span recall@k` — в топ-k есть чанк, содержащий `gold_match` (основная);
  - `loc recall@k` — в топ-k есть чанк из золотой локации (мягкая);
  - `MRR` — по span-попаданию.
- **Конфигурации**: три стратегии чанкинга × {гибрид BM25+e5+RRF,
  гибрид + reranker bge-reranker-v2-m3 (top-30 → top-10)}.
  Запросы расширяются алиасами персонажей (configs/characters.yaml).

## Результаты

(таблица заполняется прогоном `python eval/run_eval.py --rerank`)

## Выводы

(после прогона)

## Ограничения

- `chapter`-стратегия имеет завышенный loc/span recall по построению
  (чанк = целая глава), но такие чанки не влезают в контекст LLM без
  усечения и обрезаются эмбеддером на 512 токенах — смотреть вместе
  с качеством конечных ответов.
- Faithfulness конечных ответов (LLM-judge / проверка цитат) — после
  подключения LLM-ключа; методика: доля утверждений ответа, дословно
  подтверждённых процитированными фрагментами.
